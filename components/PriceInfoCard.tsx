import Image from "next/image";

interface Props {
    title: string;
    iconSec: string;
    value: string;


const PriceInfoCard = (Props) => {
  return (
    <div className=";pl.."
  )
}

export default PriceInfoCard